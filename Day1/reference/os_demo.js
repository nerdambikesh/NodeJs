const os = require('os');

// Plaform of operating system.
console.log(os.platform());
// CPU Arch
console.log(os.arch());
// Free memory
console.log(os.freemem());
// Total memtory

console.log(os.totalmem());

// Home dir
console.log(os.homedir());

// Uptim e

console.log(os.uptime());